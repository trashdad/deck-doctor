# SP11 SCAFFOLD — implement per roadmap §SP11.
# python:3.13-slim; copy backend/ only (self-contained — scoring/ is offline tooling);
# pip install -r backend/requirements.txt;
# data is volume-mounted at /app/data (SIMMANDER_* env vars point there; userdecks goes to
# a WRITABLE mount, e.g. SIMMANDER_USERDECKS=/app/userdata/userdecks.sqlite).
# CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001"]
FROM python:3.13-slim
# TODO(SP11): COPY backend/ /app/backend, install, WORKDIR /app/backend, CMD uvicorn.
